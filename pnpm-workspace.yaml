packages:
  - "."

# Build scripts approved to run on install (pnpm 11 `allowBuilds`).
# Without this, pnpm blocks install scripts and fails EVERY `pnpm <script>`
# with ERR_PNPM_IGNORED_BUILDS — typecheck included, not just build.
allowBuilds:
  "@parcel/watcher": true
  esbuild: true
